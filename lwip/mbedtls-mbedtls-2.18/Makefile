ifneq ($(wildcard $(MBEDTLS_PATH)),)

include $(MBEDTLS_PATH)/FileLists.mk
C_SRCDIRS += $(addprefix $(MBEDTLS_PATH)/,$(MBEDTLS_C_SRCDIRS))

INCDIRS += $(addprefix $(MBEDTLS_PATH)/,$(MBEDTLS_INCDIRS))

COMMON_FLAGS += -DMBEDTLS_HW -DMBEDTLS_HW_ECC -DMBEDTLS_HW_RSA -DMBEDTLS_HW_AES -DMBEDTLS_HW_HASH
endif